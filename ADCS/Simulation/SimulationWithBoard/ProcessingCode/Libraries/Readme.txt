The project needs 2 libraries to work:

-gwoptics. To do that copy the files into processing->libraries (can be found in file ->preferences). 
-PeasyCam. Use processing to download and install the library “PeasyCam” (sketch->import a library->add a library->search “PeasyCam”->install)